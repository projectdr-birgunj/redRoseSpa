Use thos command to generate ssh key

ssh-keygen -t rsa

press enter to bypass all entries

Then paste the entries of id_rsa.pub file in GitHub in SSH and GFG section of setting

 Use push to push your changes
